// Deprecated legacy theme. Left as empty stub to avoid breaking old imports while migrating.
// Remove this file entirely once all legacy references are gone.
export const theme = {} as const;
export type Theme = Record<string, never>;
