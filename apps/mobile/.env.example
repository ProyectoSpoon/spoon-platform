# Variables de entorno para Spoon (React Native / Expo)
# Copia este archivo a `.env` y rellena con los valores reales.

# Si usas Expo, prefija con EXPO_PUBLIC_ para exponerlas al bundle.
EXPO_PUBLIC_SUPABASE_URL=https://TU_PROYECTO.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=TU_CLAVE_ANON

# API WebAdmin (backend central de restaurantes)
EXPO_PUBLIC_WEBADMIN_API=http://localhost:3000

# Claves no públicas (solo en backend / build server)
# SUPABASE_SERVICE_ROLE_KEY=
